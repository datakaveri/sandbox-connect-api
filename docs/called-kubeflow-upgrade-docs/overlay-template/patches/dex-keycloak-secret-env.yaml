apiVersion: apps/v1
kind: Deployment
metadata:
  name: dex
  namespace: auth
spec:
  template:
    spec:
      containers:
        - name: dex
          envFrom:
            - secretRef:
                name: dex-oidc-client
            - secretRef:
                name: keycloak-dex-connector
